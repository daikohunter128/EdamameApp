<%= f.fields_for :history, f.object.history do |ff| %>
  <%= markup do |m|
    p = HistoryFormPresenter.new(ff, self)
    p.with_options(required: true) do |q|
      m << q.drop_down_list_block(:reservation, '変更後の注文数量', History::RESERVATION)
    end
  end %>
<% end %>